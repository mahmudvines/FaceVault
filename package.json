{
  "name": "facevault",
  "version": "1.0.0",
  "private": true,
  "description": "FaceVault local face recognition Flask app",
  "scripts": {
    "start": "python start.py",
    "setup": "python -m pip install -r requirements.txt"
  }
}
